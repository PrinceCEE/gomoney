import { setupAuthRoutes } from "./auth";
import { setupFixturesRoutes } from "./fixtures";
import { setupTeamsRoutes } from "./teams";

export const routers = { setupAuthRoutes, setupFixturesRoutes, setupTeamsRoutes };
